#!/usr/bin/env node

/**
 * Entry point for ollama-agent v2
 *
 * This is a placeholder implementation that will be replaced
 * with actual CLI parsing and agent orchestration.
 */

// eslint-disable-next-line no-console
console.log("Hello, World!");
process.exit(0);
